import { HoverClickDirective } from './hover-click.directive';

describe('HoverClickDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverClickDirective();
    expect(directive).toBeTruthy();
  });
});
